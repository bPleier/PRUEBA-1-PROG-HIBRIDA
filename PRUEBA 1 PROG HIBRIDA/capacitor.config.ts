import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.v1.dilan.gatica',
  appName: 'evaluacion-1-dilan-gatica',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
